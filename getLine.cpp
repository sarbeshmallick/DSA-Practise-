#include <iostream>
using namespace std;

int main() {

  string str;
  getline(cin, str);
  cout << "Life is Enjoy " << str;
    
    return 0;
}

// as in getLine1 we have given the option to take input and then output it with our (Life is Enjoy) + user input  

// but in getLine2 whatever user inputs in the terminal will get printed as output. We have also added a string "Life is Enjoy " in getLine1 before the user input to make the output more meaningful.